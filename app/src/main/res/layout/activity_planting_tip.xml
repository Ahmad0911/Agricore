<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="?attr/colorSurface"
    android:foreground="?android:attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Header Section -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Title and Expand Icon Row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tv_tip_title"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:textColor="?attr/colorPrimary"
                    android:lineSpacing="1.2"
                    tools:text="Best Time to Plant Tomatoes" />

                <ImageView
                    android:id="@+id/iv_expand_icon"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@drawable/ic_expand_more"
                    android:contentDescription="@string/expand_icon_desc"
                    app:tint="?attr/colorSecondary" />

            </LinearLayout>

            <!-- Seasonal Badge (shows if current season) -->
            <TextView
                android:id="@+id/tv_seasonal_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:paddingHorizontal="12dp"
                android:paddingVertical="4dp"
                android:background="@drawable/bg_seasonal_badge"
                android:text="@string/current_season"
                android:textSize="10sp"
                android:textStyle="bold"
                android:textColor="@android:color/white"
                android:visibility="gone"
                tools:visibility="visible" />

            <!-- Description -->
            <TextView
                android:id="@+id/tv_tip_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:textSize="14sp"
                android:lineSpacing="1.3"
                android:textColor="?android:attr/textColorPrimary"
                tools:text="Tomatoes thrive in warm weather and should be planted after the last frost date in your area. The soil temperature should be at least 60°F (15°C) for optimal germination." />

            <!-- Category and Details Chips -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:orientation="horizontal"
                android:gravity="start">

                <TextView
                    android:id="@+id/tv_category_chip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="8dp"
                    android:paddingHorizontal="12dp"
                    android:paddingVertical="6dp"
                    android:background="@drawable/bg_chip_category"
                    android:textSize="12sp"
                    android:textColor="?attr/colorOnSecondary"
                    tools:text="Vegetables" />

                <TextView
                    android:id="@+id/tv_season_chip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="8dp"
                    android:paddingHorizontal="12dp"
                    android:paddingVertical="6dp"
                    android:background="@drawable/bg_chip_season"
                    android:textSize="12sp"
                    android:textColor="?attr/colorOnSecondary"
                    tools:text="Spring" />

                <TextView
                    android:id="@+id/tv_difficulty_chip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingHorizontal="12dp"
                    android:paddingVertical="6dp"
                    android:background="@drawable/bg_chip_difficulty"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    tools:text="Beginner" />

            </LinearLayout>

        </LinearLayout>

        <!-- Expandable Tips Section -->
        <LinearLayout
            android:id="@+id/ll_tips_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="16dp"
            android:paddingBottom="16dp"
            android:visibility="gone"
            tools:visibility="visible">

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginBottom="12dp"
                android:background="?android:attr/listDivider"
                android:alpha="0.3" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/detailed_tips"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="?attr/colorSecondary"
                android:layout_marginBottom="8dp" />

            <!-- Tips will be dynamically added here by the adapter -->

        </LinearLayout>

        <!-- Quick Action Buttons (Optional) -->
        <LinearLayout
            android:id="@+id/ll_action_buttons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingHorizontal="8dp"
            android:paddingBottom="8dp"
            android:gravity="end"
            android:visibility="gone"
            tools:visibility="visible">

            <Button
                android:id="@+id/btn_bookmark"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="@string/bookmark"
                android:textSize="12sp"
                android:drawableStart="@drawable/ic_bookmark_border"
                android:drawablePadding="4dp"
                android:paddingHorizontal="12dp"
                android:minWidth="0dp" />

            <Button
                android:id="@+id/btn_share"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:layout_marginStart="8dp"
                android:text="@string/share"
                android:textSize="12sp"
                android:drawableStart="@drawable/ic_share"
                android:drawablePadding="4dp"
                android:paddingHorizontal="12dp"
                android:minWidth="0dp" />

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>